global:
  postgresql:
    auth:
      username: "backstage"
      postgresPassword: "backstage"
      password: "backstage"
      database: "backstage"
primary:
  name: primary
  resources:
    requests:
      memory: 256Mi
      cpu: 100m
  persistence:
    enabled: true
    size: 8Gi
